import {
    StyleSheet
} from 'react-native';

const LoginStyles = StyleSheet.create({
    loginBtn:{
        marginTop: 30,
        marginBottom:10,
        alignItems: 'center',
        backgroundColor: 'green',
        borderRadius: 5,
        width: '100%',
    },
    loginView :{
        marginTop:50,
        marginBottom : 10,
        alignContent: 'center',
    },
    signupBtn: {
        width: '100%',
    }
});
export default LoginStyles;